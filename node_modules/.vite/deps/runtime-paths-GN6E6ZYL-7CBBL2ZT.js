import {
  Ct,
  Di,
  Et,
  Fe,
  Fi,
  It,
  Lt,
  Me,
  Vi,
  ki,
  lr,
  we
} from "./chunk-EXQGW6OA.js";
import "./chunk-KUL3PBD7.js";
import "./chunk-HGU3ZNVU.js";
import {
  Zt,
  mt
} from "./chunk-XTM2MF4D.js";
import "./chunk-PIDU3GFS.js";
import "./chunk-U56OWULI.js";
import "./chunk-2C37H3P5.js";
import {
  a,
  i,
  v
} from "./chunk-EPYBTGE4.js";
import {
  cr,
  hr,
  vr
} from "./chunk-NCELNTZW.js";
import {
  As,
  Gi,
  Ki,
  Zi,
  ha,
  qr,
  ss
} from "./chunk-G2UECJUH.js";
import "./chunk-Y6SG2FYX.js";
import "./chunk-EQCVQC35.js";

// node_modules/@splinetool/runtime/build/runtime-paths-GN6E6ZYL.js
var A = class extends lr {
  constructor(e, t, s) {
    super(e, t, s), this.isPathObject = true, this._shapeId = null, this._lastShapeParams = null, this._lastShape = null, this._context = s;
  }
  updateState(e, t) {
    super.updateState(e, t), this.updateShape(), this.refreshAttachedCloners(t);
  }
  updateShape() {
    var _a;
    let t, s = { ...this.data.geometry.extrusion.shape };
    for (let e in s) "string" == typeof s[e] && (s[e] = this._context.shared.getVariable(s[e], [this.uuid, "geometry", "extrusion", "shape", e]));
    if ("Custom" === s.type) {
      let e = s.shapeId;
      if (e !== this._shapeId && this.detachShape(), e) {
        this._shapeId = e;
        let s2 = this._context.scene.find(e);
        (s2 == null ? void 0 : s2.data) && (s2.attachedPaths.add(this), t = (_a = s2.geometry.userData) == null ? void 0 : _a.shape), t || this._context.scene.addPendingCommand(() => this.updateShape());
      }
    } else if (null !== this._lastShape && this._sameShapeParams(s)) t = this._lastShape;
    else {
      let i3;
      switch (s.type) {
        case "Rectangle":
          i3 = Fi;
          break;
        case "Ellipse":
          i3 = Di;
          break;
        case "Polygon":
          i3 = ki;
          break;
        case "Star":
          i3 = Vi;
          break;
        default:
          throw new Error(`Unknown shape type: ${s.type}`);
      }
      t = i3.create({ parameters: s }).userData.shape, this._lastShapeParams = s, this._lastShape = t ?? null;
    }
    let i2 = this.geometry;
    t && i2.inputs && (i2.inputs.shapeData = t, i2.build());
  }
  _sameShapeParams(e) {
    let t = this._lastShapeParams;
    if (null === t) return false;
    for (let s in e) if (!Object.is(e[s], t[s])) return false;
    for (let s in t) if (!(s in e)) return false;
    return true;
  }
  detachShape() {
    var _a;
    null !== this._shapeId && ((_a = this._context.scene.find(this._shapeId)) == null ? void 0 : _a.attachedPaths.delete(this));
  }
  supportsInPlaceGeometryUpdate() {
    return false;
  }
  createGeometryDelayed(e) {
    this.geometryCreateDeleyed = e.shared, this.updateShape(), this.refreshAttachedPaths(e);
  }
  updateTransformState(e, t) {
    return super.updateTransformState(e, t);
  }
  updateGeometryInteractions(e, t) {
    super.updateGeometryInteractions(e, t), this.updateShape();
  }
  updateEntityBoxSize(e, t) {
    let s = this.geometry.getAttribute("position");
    void 0 !== s && s.count > 0 ? Zt(s, this.geometry.drawRange.start, this.geometry.drawRange.count < 1 / 0 ? this.geometry.drawRange.count : s.count, e, t) : super.updateEntityBoxSize(e, t);
  }
};
var D = new As();
var O = new As();
var k = new As();
var I = new Ki();
var T = new Ki();
var R = new Ki();
var E = new Ki();
var z = new Ki();
var j = new Ki();
var U = new Ki();
var G = /* @__PURE__ */ new WeakMap();
var F = /* @__PURE__ */ new WeakMap();
var B = /* @__PURE__ */ new WeakMap();
function V(e) {
  e.setIndex(null), e.setAttribute("position", new qr(new Float32Array(0), 3)), e.setAttribute("normal", new qr(new Float32Array(0), 3)), e.setAttribute("uv", new qr(new Float32Array(0), 2)), e.setDrawRange(0, 0);
}
var N = class extends ha {
  constructor(e) {
    super(), this.type = "PathExtrusionGeometry", this.inputs = e, this.build();
  }
  _isGeometryClosed() {
    return this.inputs.path.isClosed && 1 === this.inputs.parameters.extrusion.depth;
  }
  _isOpenEnded() {
    let e = this.inputs.parameters.extrusion;
    return !(this.inputs.path.isClosed && 1 === e.depth && (function(e2) {
      let t = 2 * Math.PI;
      return (e2 % t + t) % t === (0 % t + t) % t;
    })(e.twist) && e.startScale === e.endScale);
  }
  build() {
    let { points: e, matrices: t } = this._getSampledPath();
    if (e.length < 2) return void V(this);
    let { depth: s, offset: i2 } = this.inputs.parameters.extrusion;
    s = Math.max(0, Math.min(s, 1)), i2 = Math.max(0, Math.min(i2, 1));
    let n = this.inputs.path.isClosed ? e.length : e.length - 1, a2 = Math.floor(i2 * n), r = this.inputs.path.isClosed ? Math.ceil((s + i2) * n) : Math.ceil(Math.min(1, s + i2) * n), o = Math.min(Math.max(2, r - a2 + 1), n + 2), h = [], u = [];
    for (let s2 = 0; s2 < o; s2++) {
      let i3 = this.inputs.path.isClosed ? (s2 + a2) % e.length : Math.min(s2 + a2, e.length - 1);
      h.push(e[i3].clone()), u.push(t[i3].clone());
    }
    let p = (e2, t2, s2) => {
      h[e2] = h[e2].clone().lerp(h[t2], s2), u[e2] = (function(e3, t3, s3) {
        let i3 = [new Ki(), new Ki(), new Ki()], n2 = [new Ki(), new Ki(), new Ki()];
        return e3.extractBasis(i3[0], i3[1], i3[2]), t3.extractBasis(n2[0], n2[1], n2[2]), i3.forEach((e4, t4) => {
          let i4 = n2[t4], a3 = Zi.lerp(e4.length(), i4.length(), s3);
          e4.lerp(i4, s3).setLength(a3);
        }), new As().makeBasis(i3[0], i3[1], i3[2]);
      })(u[e2], u[t2], s2);
    }, c = 0, d = i2 * n % 1;
    (!this.inputs.path.isClosed || s <= 1) && (d || 0 === i2) && (c = d, p(0, 1, c));
    let m = 0, g = (i2 + s) * n % 1;
    if ((this.inputs.path.isClosed && s <= 1 || !this.inputs.path.isClosed && i2 + s < 1) && g && (m = g, p(h.length - 1, h.length - 2, 1 - m)), 0 === s) {
      let e2 = h.length - 1;
      h[e2].copy(h[0]), u[e2].copy(u[0]);
    }
    this._applyPathModifiers(u, c, m);
    let { bevel: y, bevelSides: f } = this.inputs.parameters.extrusion, v2 = y > 0 ? this.inputs.parameters.extrusion.capType : "flat", w = 5;
    "Custom" === this.inputs.parameters.extrusion.shape.type && (w = "low" === this.inputs.parameters.extrusion.shape.shapeQuality ? 5 : 12);
    let S, { regions: _, infos: M, vertices: A2 } = this._getShapePoints(w), k2 = 0;
    if ("round" === v2) {
      let e2 = this.inputs.shapeData, t2 = B.get(e2);
      void 0 !== t2 && t2.bevel === y && t2.curveSegments === w && t2.bevelSides === f ? S = t2.geometry : (S = new Lt(e2, 2 * y, y, w, f, void 0, true), B.set(e2, { bevel: y, curveSegments: w, bevelSides: f, geometry: S })), k2 = S.getAttribute("position").count;
    }
    let I2 = 0, T2 = 0;
    M.sort((e2, t2) => e2.start - t2.start), M.forEach((e2) => {
      e2.verticesStart = I2, e2.verticesCount = e2.continuous.reduce((e3, t2, s2) => e3 + (0 !== s2 && t2 ? 1 : 2), 0), T2 += e2.verticesCount, I2 = T2;
    });
    let R2, E2 = T2 * o, z2 = 0;
    this._isOpenEnded() && "flat" === v2 && (R2 = this._getSurfaceTesselation(_), z2 = R2.vertexCount);
    let j2 = E2 + 2 * z2 + 2 * k2, U2 = E2 + 2 * z2, G2 = { positions: new Float32Array(3 * j2), normals: new Float32Array(3 * j2), uvs: new Float32Array(2 * j2) }, F2 = S ? S.getIndex().array : null, N2 = 0;
    M.forEach((e2) => {
      N2 += e2.count;
    });
    let H = N2 * (o - 1) * 6 + (R2 ? 2 * R2.elementCount * 3 : 0) + (F2 ? 2 * F2.length : 0), q = new Uint32Array(H), X = 0;
    if (M.forEach((e2) => {
      X = this._extrudeRegion(e2, A2, u, h, G2, q, X, this._isGeometryClosed() && !this._isOpenEnded());
    }), R2 && (X = this._closeEnd(R2, E2, q, X, G2, u[0], h[0], false), X = this._closeEnd(R2, E2 + z2, q, X, G2, u[u.length - 1], h[h.length - 1], true)), S) {
      G2.positions.set(S.getAttribute("position").array, 3 * U2), G2.normals.set(S.getAttribute("normal").array, 3 * U2), G2.uvs.set(S.getAttribute("uv").array, 2 * U2);
      for (let e3 = U2; e3 < U2 + k2; e3++) G2.uvs[2 * e3 + 1] = 1e-4;
      let e2 = X;
      for (let e3 = 0; e3 < F2.length; e3++) q[X++] = F2[e3] + U2;
      U2 += k2, G2.positions.set(S.getAttribute("position").array, 3 * U2), G2.normals.set(S.getAttribute("normal").array, 3 * U2), G2.uvs.set(S.getAttribute("uv").array, 2 * U2);
      let t2 = X;
      for (let e3 = 0; e3 < F2.length; e3++) q[X++] = F2[e3] + U2;
      this.setAttribute("position", new qr(G2.positions, 3)), this.setAttribute("normal", new qr(G2.normals, 3)), this.setAttribute("uv", new qr(G2.uvs, 2)), this._setIndexFromUint32(q);
      let s2 = D;
      s2.copy(u[u.length - 1]).setPosition(h[h.length - 1]), this.applyMatrix4OnRange(s2, U2, j2), s2.copy(u[0]).setPosition(h[0]).multiply(O.makeScale(1, 1, -1)), this.applyMatrix4OnRange(s2, U2 - k2, U2), this.reverseIndicesOnRange(e2, t2);
    } else this.setAttribute("position", new qr(G2.positions, 3)), this.setAttribute("normal", new qr(G2.normals, 3)), this.setAttribute("uv", new qr(G2.uvs, 2)), this._setIndexFromUint32(q);
  }
  _setIndexFromUint32(e) {
    let t = false;
    for (let s = e.length - 1; s >= 0; --s) if (e[s] >= 65535) {
      t = true;
      break;
    }
    this.setIndex(new qr(t ? e : new Uint16Array(e), 1));
  }
  _getSampledPath() {
    let e = this.inputs.path, t = mt(e), s = G.get(t);
    if (void 0 === s || s.subdivisions !== e.subdivisions || s.subdivisionMode !== e.subdivisionMode) {
      let i2 = this._extractPathPoints(t), n = i2.length < 2 ? [] : this._computeBasisMatrices(i2);
      s = { subdivisions: e.subdivisions, subdivisionMode: e.subdivisionMode, points: i2, matrices: n }, G.set(t, s);
    }
    return s;
  }
  _extractPathPoints(e) {
    let t = this.inputs.path.subdivisions, s = "uniform" === this.inputs.path.subdivisionMode ? e.getSpacedPoints(4 * t) : e.getPoints(t);
    if (s.length < 2) return [];
    let i2 = [s[0]];
    return s.forEach((e2) => {
      i2[i2.length - 1].distanceToSquared(e2) > 1e-3 && i2.push(e2);
    }), this.inputs.path.isClosed && i2[i2.length - 1].distanceTo(i2[0]) < 1e-3 && i2.pop(), i2;
  }
  _computeBasisMatrices(e) {
    let t = [], s = e.length, i2 = this.inputs.path.isClosed, n = new Ki(), a2 = new Ki(), r = new Ki(), o = new Ki(), l = new Ki(0, 1, 0);
    for (let h2 = 0; h2 < s; h2++) {
      let u2, p2, c2 = e[h2];
      u2 = 0 === h2 ? i2 ? e[e.length - 2] : I.copy(c2).multiplyScalar(2).sub(e[1]) : e[h2 - 1], p2 = h2 === s - 1 ? i2 ? e[1] : I.copy(c2).multiplyScalar(2).sub(e[h2 - 1]) : e[h2 + 1], T.subVectors(c2, u2).normalize(), R.subVectors(p2, c2).normalize();
      let d2 = E.addVectors(T, R).normalize();
      r.copy(d2), 0 === h2 && (d2.equals(l) || z.copy(d2).negate().equals(l)) && l.set(0, 0, 1);
      let m = j.crossVectors(l, d2).normalize(), g = U.crossVectors(d2, m).normalize();
      l.copy(g), o.copy(m), 0 === h2 && (n.copy(g), a2.copy(d2));
      let y = new As().makeBasis(m, g, d2);
      t.push(y);
    }
    let h = i2 ? a2 : r, u = i2 ? n : new Ki(0, 1, 0), p = h.clone().cross(o).normalize(), c = Math.acos(u.dot(p));
    if (isNaN(c)) return t;
    let d = u.clone().cross(p);
    h.dot(d) > 0 && (c *= -1);
    for (let e2 = 1; e2 < t.length; e2++) t[e2].multiply(k.makeRotationZ(c * e2 / t.length));
    return t;
  }
  _applyPathModifiers(e, t, s) {
    let i2 = e.length, { angle: n, twist: a2, startScale: r, endScale: o } = this.inputs.parameters.extrusion, l = new As(), h = new As();
    return e.forEach((e2, u) => {
      let p = 0 === u ? 0 : u === i2 - 1 ? 1 : (u - t) / (i2 - (0 === s ? 0 : 1) - (t + (1 - s)));
      l.makeRotationZ(Zi.lerp(n, n + a2, p) * Zi.DEG2RAD);
      let c = Zi.lerp(r, o, p);
      h.makeScale(c, c, c), e2.multiply(l).multiply(h);
    }), e;
  }
  _getShapePoints(e, t = we.ODD) {
    let i2 = this.inputs.shapeData, n = F.get(i2);
    return (void 0 === n || n.curveSegments !== e || n.windingRule !== t) && (n = { curveSegments: e, windingRule: t, shapePoints: this._computeShapePoints(e, t) }, F.set(i2, n)), n.shapePoints;
  }
  _getSurfaceTesselation(e) {
    let t, a2 = F.get(this.inputs.shapeData);
    if (void 0 !== (a2 == null ? void 0 : a2.surfaceTesselation)) return a2.surfaceTesselation;
    try {
      t = Fe({ windingRule: we.ODD, elementType: Me.POLYGONS, polySize: 3, vertexSize: 2, strict: true, contours: e });
    } catch {
      t = It;
    }
    return void 0 !== a2 && (a2.surfaceTesselation = t), t;
  }
  _computeShapePoints(e = 12, t = we.ODD) {
    let o, l, h = this.inputs.shapeData, u = h.extractShapePointsToFlatArray([], e), p = h.shapeHoles.map((t2) => {
      let s = t2.extractShapePointsToFlatArray([], e), i2 = [];
      for (let e2 = s.length - 1; e2 >= 1; e2 -= 2) {
        let t3 = s[e2 - 1], n = s[e2 - 0];
        i2.push(t3, n);
      }
      return i2;
    });
    try {
      o = Fe({ windingRule: t, elementType: Me.BOUNDARY_CONTOURS, vertexSize: 2, strict: true, contours: [u] });
    } catch {
      o = Ct;
    }
    try {
      l = Fe({ windingRule: we.ODD, elementType: Me.BOUNDARY_CONTOURS, vertexSize: 2, strict: true, contours: [...p] });
    } catch {
      l = Et;
    }
    if (!o) throw new Error("error generating geometry");
    let c = o.elementCount;
    if (l) {
      o.elementCount += l.elementCount;
      for (let e2 = 0; e2 < l.elements.length; e2++) {
        let t2 = l.elements[e2], s = e2 % 2 == 0 ? o.vertexCount : 0;
        o.elements.push(t2 + s);
      }
      for (let e2 = 0; e2 < l.vertexIndices.length; e2++) {
        let t2 = l.vertexIndices[e2], s = o.vertexCount;
        o.vertexIndices.push(t2 + s);
      }
      for (let e2 = 0; e2 < l.vertices.length; e2++) {
        let t2 = l.vertices[e2];
        o.vertices.push(t2);
      }
    }
    let d = 1 / 0, m = -1 / 0, g = 1 / 0, y = -1 / 0;
    for (let e2 = 0, t2 = o.vertexCount; e2 < t2; e2++) {
      let t3 = 2 * e2, s = o.vertices[t3 + 0], i2 = o.vertices[t3 + 1];
      s < d && (d = s), s > m && (m = s), i2 < g && (g = i2), i2 > y && (y = i2);
    }
    let f = [];
    for (let e2 = o.elementCount - 1; e2 >= 0; e2--) {
      let t2 = e2 >= c, s = 2 * e2, i2 = o.elements[s + 0], n = o.elements[s + 1], a2 = i2 + n, r = { start: i2, count: n, normals: [], isHole: t2, continuous: [], verticesStart: 0, verticesCount: 0 };
      f.push(r);
      let l2 = i2, u2 = a2 - 1, p2 = i2 + 1, d2 = h.roundedCurves.length;
      do {
        let e3 = l2 - i2, s2 = o.vertices[2 * u2 + 0], c2 = o.vertices[2 * u2 + 1], m2 = o.vertices[2 * l2 + 0], g2 = o.vertices[2 * l2 + 1], y2 = o.vertices[2 * p2 + 0], f2 = o.vertices[2 * p2 + 1], v2 = m2 - s2, w = g2 - c2, x = Math.sqrt(v2 * v2 + w * w);
        v2 /= x, w /= x;
        let S = m2 - y2, b = g2 - f2, _ = Math.sqrt(S * S + b * b);
        S /= _, b /= _, r.normals[2 * e3 + 0] = -b, r.normals[2 * e3 + 1] = S;
        let P = o.vertexIndices[l2];
        if (Array.isArray(P)) r.continuous[e3] = false;
        else {
          let [t3, s3] = h.getCurveIndexFromVertexId(P - 1, true);
          if (s3 > 0 && s3 < 1) r.continuous[e3] = true;
          else {
            let i3 = 1 === s3 ? t3 + 1 : t3 - 1;
            i3 = (i3 + d2) % d2;
            let n2 = 1 === s3 ? 0 : 1, a3 = h.roundedCurves[t3].getTangent(s3), o2 = h.roundedCurves[i3].getTangent(n2);
            r.continuous[e3] = a3.dot(o2) > 0.95;
          }
        }
        t2 && (r.normals[2 * e3 + 0] *= -1, r.normals[2 * e3 + 1] *= -1), [u2, l2, p2] = [l2, p2, p2 + 1], p2 >= a2 && (p2 -= n);
      } while (p2 !== i2 + 1);
    }
    return { regions: [u, ...p], infos: f, vertices: o.vertices };
  }
  _insertVertex(e, t, s, i2, n) {
    let a2 = 2 * t, r = 3 * t;
    e.positions[r + 0] = s.x, e.positions[r + 1] = s.y, e.positions[r + 2] = s.z, e.normals[r + 0] = i2.x, e.normals[r + 1] = i2.y, e.normals[r + 2] = i2.z, e.uvs[a2 + 0] = n.x, e.uvs[a2 + 1] = n.y;
  }
  _extrudeRegion(e, t, s, i2, n, a2, r, o) {
    let l = e.wallTemplate;
    if (void 0 === l) {
      l = [];
      for (let s2 = 0; s2 < e.count; s2++) {
        let i3, n2, a3 = 2 * (e.start + s2), r2 = t[a3 + 0], o2 = t[a3 + 1];
        if (e.continuous[s2]) i3 = e.normals[2 * s2 + 0], n2 = e.normals[2 * s2 + 1];
        else {
          let l2 = 0 === s2 ? 2 * (e.start + e.count - 1) : a3 - 2, h3 = r2 - t[l2 + 0];
          i3 = -(o2 - t[l2 + 1]), n2 = h3, e.isHole || (i3 = -i3, n2 = -n2);
        }
        let h2 = 0 === s2 ? 1 : s2 / e.count;
        if (l.push(r2, o2, i3, n2, h2, 0), !e.continuous[s2] || 0 === s2) if (0 === s2) l.push(r2, o2, e.normals[0], e.normals[1], 0, 1);
        else {
          let u2 = s2 === e.count - 1 ? 2 * e.start : a3 + 2, p2 = t[u2 + 0] - r2;
          i3 = -(t[u2 + 1] - o2), n2 = p2, e.isHole || (i3 = -i3, n2 = -n2), l.push(r2, o2, i3, n2, h2, 1);
        }
      }
      e.wallTemplate = l;
    }
    let h = s.length, u = l.length, p = n.positions, c = n.normals, d = n.uvs;
    for (let t2 = 0; t2 < h; t2++) {
      let n2 = s[t2].elements, a3 = n2[0], r2 = n2[1], o2 = n2[2], m2 = n2[4], g2 = n2[5], y2 = n2[6], f2 = n2[12], v2 = n2[13], w = n2[14], x = i2[t2], S = t2 / (h - 1), b = 0, _ = 0, P = 0, C = e.verticesStart * h + e.verticesCount * t2;
      for (let e2 = 0; e2 < u; e2 += 6) {
        if (0 === l[e2 + 5]) {
          let t4 = l[e2 + 0], s3 = l[e2 + 1];
          b = a3 * t4 + m2 * s3 + f2 + x.x, _ = r2 * t4 + g2 * s3 + v2 + x.y, P = o2 * t4 + y2 * s3 + w + x.z;
        }
        let t3 = l[e2 + 2], s2 = l[e2 + 3], i3 = a3 * t3 + m2 * s2 + f2, n3 = r2 * t3 + g2 * s2 + v2, h2 = o2 * t3 + y2 * s2 + w, u2 = 1 / (Math.sqrt(i3 * i3 + n3 * n3 + h2 * h2) || 1);
        i3 *= u2, n3 *= u2, h2 *= u2;
        let M = 2 * C, A2 = 3 * C;
        p[A2 + 0] = b, p[A2 + 1] = _, p[A2 + 2] = P, c[A2 + 0] = i3, c[A2 + 1] = n3, c[A2 + 2] = h2, d[M + 0] = l[e2 + 4], d[M + 1] = S, C++;
      }
    }
    let m = e.wallQuadPattern;
    if (void 0 === m) {
      m = [];
      let t2 = 0;
      for (let s2 = 0; s2 < e.count; s2++) (!e.continuous[s2] || 0 === s2) && t2++, m.push(t2, s2 === e.count - 1 ? 0 : t2 + 1), t2++;
      e.wallQuadPattern = m;
    }
    let g = h - 1, y = m.length, f = e.isHole;
    for (let t2 = 0; t2 < g; t2++) {
      let s2 = e.verticesStart * h + e.verticesCount * t2, i3 = s2 + e.verticesCount;
      for (let e2 = 0; e2 < y; e2 += 2) {
        let t3 = s2 + m[e2], n2 = s2 + m[e2 + 1], o2 = i3 + m[e2 + 1], l2 = i3 + m[e2];
        f ? (a2[r++] = t3, a2[r++] = o2, a2[r++] = n2, a2[r++] = t3, a2[r++] = l2, a2[r++] = o2) : (a2[r++] = t3, a2[r++] = n2, a2[r++] = o2, a2[r++] = t3, a2[r++] = o2, a2[r++] = l2);
      }
    }
    return r;
  }
  _closeEnd(e, t, s, i2, n, a2, r, o) {
    let l = e.vertexCount, h = new Ki(0, 0, o ? -1 : 1).applyMatrix4(a2), u = new Ki(), p = new Gi();
    for (let s2 = 0; s2 < l; s2++) {
      let i3 = 2 * s2;
      u.set(e.vertices[i3 + 0], e.vertices[i3 + 1], 0).applyMatrix4(a2).add(r), this._insertVertex(n, t + s2, u, h, p);
    }
    let c = e.elements;
    for (let n2 = 0; n2 < e.elementCount; n2++) {
      let e2 = 3 * n2;
      s[i2++] = c[e2 + 0] + t, s[i2++] = c[e2 + (o ? 1 : 2)] + t, s[i2++] = c[e2 + (o ? 2 : 1)] + t;
    }
    return i2;
  }
  applyMatrix4OnRange(e, t, s) {
    let i2, n, a2, r, o = e.elements, l = new ss().getNormalMatrix(e).elements, h = this.attributes.position, u = this.attributes.normal;
    if (!h || !u) return;
    let p = h.array, c = u.array, d = h.itemSize;
    for (let e2 = t * d, h2 = s * d; e2 < h2; e2 += d) i2 = p[e2 + 0], n = p[e2 + 1], a2 = p[e2 + 2], r = 1 / (o[3] * i2 + o[7] * n + o[11] * a2 + o[15]), p[e2 + 0] = (o[0] * i2 + o[4] * n + o[8] * a2 + o[12]) * r, p[e2 + 1] = (o[1] * i2 + o[5] * n + o[9] * a2 + o[13]) * r, p[e2 + 2] = (o[2] * i2 + o[6] * n + o[10] * a2 + o[14]) * r, i2 = c[e2 + 0], n = c[e2 + 1], a2 = c[e2 + 2], c[e2 + 0] = l[0] * i2 + l[3] * n + l[6] * a2, c[e2 + 1] = l[1] * i2 + l[4] * n + l[7] * a2, c[e2 + 2] = l[2] * i2 + l[5] * n + l[8] * a2;
    h.needsUpdate = true, u.needsUpdate = true;
  }
  reverseIndicesOnRange(e, t) {
    let s = this.index;
    if (s) {
      for (let i2 = e; i2 < t; i2 += 3) {
        let e2 = s.getX(i2), t2 = s.getX(i2 + 1), n = s.getX(i2 + 2);
        s.setXYZ(i2, n, t2, e2);
      }
      s.needsUpdate = true;
    }
  }
};
i("PathGeometry", (e, t, s) => new A(e, t, s)), v("PathGeometry", class {
  static create(e) {
    return this.build(this.normalizeInputs(e));
  }
  static normalizeInputs(e, t) {
    let s = Object.assign({}, (t == null ? void 0 : t.parameters) ?? { width: 100, height: 100, depth: 1, subdivisions: 12, roundness: 0, extrudeBevelSize: 0, extrudeBevelSegments: 3 }, e.parameters), i2 = Math.abs(s.width), n = Math.abs(s.height ?? s.width), a2 = Math.abs(s.depth ?? 0), r = (t == null ? void 0 : t.shapeData) ?? Di.create({ parameters: { ...hr, width: "number" == typeof hr.width ? hr.width : 100, height: "number" == typeof hr.height ? hr.height : 100 } }).userData.shape;
    return { path: e.path ?? cr.defaultData(), parameters: Object.assign(s, { width: i2, height: n, depth: a2, extrusion: { ...vr, ...s.extrusion } }), shapeData: r };
  }
  static build(e) {
    if (e.path.points.length >= 2) {
      let t = new N(e);
      return Object.assign(t, { userData: { ...e, type: "PathGeometry" } });
    }
    {
      let t = new ha();
      return V(t), Object.assign(t, { userData: { ...e, type: "PathGeometry" } });
    }
  }
}), a("3d-paths");
//# sourceMappingURL=runtime-paths-GN6E6ZYL-7CBBL2ZT.js.map
