import {
  De,
  Fo,
  Ro,
  We,
  ha,
  hs,
  ml,
  oa,
  qr,
  rr,
  ua
} from "./chunk-G2UECJUH.js";
import "./chunk-Y6SG2FYX.js";
import "./chunk-EQCVQC35.js";

// node_modules/@splinetool/runtime/build/runtime-DRACOLoader-TQHJ6SJB.js
var u = /* @__PURE__ */ new WeakMap();
var h = new URL("../libs/draco/draco_decoder.wasm", import.meta.url).toString();
var f = new URL("../libs/draco/draco_wasm_wrapper.js", import.meta.url).toString();
var w = new URL("../libs/draco/draco_decoder.js", import.meta.url).toString();
var p = { js: new URL("../libs/draco/gltf/draco_wasm_wrapper.js", import.meta.url).toString(), wasm: new URL("../libs/draco/gltf/draco_decoder.wasm", import.meta.url).toString() };
var m = class extends Ro {
  constructor(e) {
    super(e), this.decoderPaths = { js: f, wasm: h, dep_js: w }, this.decoderConfig = {}, this.decoderBinary = null, this.decoderPending = null, this.workerLimit = 4, this.workerPool = [], this.workerNextTaskID = 1, this.workerSourceURL = "", this.defaultAttributeIDs = { position: "POSITION", normal: "NORMAL", color: "COLOR", uv: "TEX_COORD" }, this.defaultAttributeTypes = { position: "Float32Array", normal: "Float32Array", color: "Float32Array", uv: "Float32Array" };
  }
  setDecoderPath(t) {
    let { decoderPaths: r } = this;
    return "object" == typeof t ? (r.js = t.js, r.wasm = t.wasm, r.dep_js = null) : (r.js = ml.resolveURL("draco_wasm_wrapper.js", t), r.wasm = ml.resolveURL("draco_decoder.wasm", t), r.dep_js = ml.resolveURL("draco_decoder.js", t)), this;
  }
  setDecoderConfig(e) {
    return console.warn("THREE.DRACOLoader: setDecoderConfig to has been deprecated and will be removed in r194."), this.decoderConfig = e, this;
  }
  setWorkerLimit(e) {
    return this.workerLimit = e, this;
  }
  load(e, t, r, o) {
    let s = new Fo(this.manager);
    s.setPath(this.path), s.setResponseType("arraybuffer"), s.setRequestHeader(this.requestHeader), s.setWithCredentials(this.withCredentials), s.load(e, (e2) => {
      this.parse(e2, t, o);
    }, r, o);
  }
  parse(e, t, o = () => {
  }) {
    this.decodeDracoFile(e, t, null, null, De, o).catch(o);
  }
  decodeDracoFile(e, t, r, s, a = We, i = () => {
  }) {
    let n = { attributeIDs: r || this.defaultAttributeIDs, attributeTypes: s || this.defaultAttributeTypes, useUniqueIDs: !!r, vertexColorSpace: a };
    return this.decodeGeometry(e, n).then(t).catch(i);
  }
  decodeGeometry(e, t) {
    let r = JSON.stringify(t);
    if (u.has(e)) {
      let t2 = u.get(e);
      if (t2.key === r) return t2.promise;
      if (0 === e.byteLength) throw new Error("THREE.DRACOLoader: Unable to re-decode a buffer with different settings. Buffer has already been transferred.");
    }
    let o, s = this.workerNextTaskID++, a = e.byteLength, i = this._getWorker(s, a).then((r2) => (o = r2, new Promise((r3, a2) => {
      o._callbacks[s] = { resolve: r3, reject: a2 }, o.postMessage({ type: "decode", id: s, taskConfig: t, buffer: e }, [e]);
    }))).then((e2) => this._createGeometry(e2.geometry));
    return i.catch(() => true).then(() => {
      o && s && this._releaseTask(o, s);
    }), u.set(e, { key: r, promise: i }), i;
  }
  _createGeometry(e) {
    let t = new ha();
    e.index && t.setIndex(new qr(e.index.array, 1));
    for (let r = 0; r < e.attributes.length; r++) {
      let o, { name: s, array: i, itemSize: l, stride: c, vertexColorSpace: u2 } = e.attributes[r];
      if (l === c) o = new qr(i, l);
      else {
        let e2 = new oa(i, c);
        o = new ua(e2, l, 0);
      }
      "color" === s && (this._assignVertexColorSpace(o, u2), o.normalized = !(i instanceof Float32Array)), t.setAttribute(s, o);
    }
    return t;
  }
  _assignVertexColorSpace(e, o) {
    if (o !== De) return;
    let a = new rr();
    for (let o2 = 0, s = e.count; o2 < s; o2++) a.fromBufferAttribute(e, o2), hs.colorSpaceToWorking(a, De), e.setXYZ(o2, a.r, a.g, a.b);
  }
  _loadLibrary(e, t) {
    let r = new Fo(this.manager);
    return r.setResponseType(t), r.setWithCredentials(this.withCredentials), new Promise((t2, o) => {
      r.load(e, t2, void 0, o);
    });
  }
  preload() {
    return this._initDecoder(), this;
  }
  _initDecoder() {
    if (this.decoderPending) return this.decoderPending;
    let e = "object" != typeof WebAssembly || "js" === this.decoderConfig.type, t = [], { decoderPaths: r } = this;
    if (e) {
      if (null === r.dep_js) throw new Error("THREE.DRACOLoader: WebAssembly is required when using a custom decoder paths.");
      t.push(this._loadLibrary(r.dep_js, "text"));
    } else t.push(this._loadLibrary(r.js, "text")), t.push(this._loadLibrary(r.wasm, "arraybuffer"));
    return this.decoderPending = Promise.all(t).then((t2) => {
      let r2 = t2[0];
      e || (this.decoderConfig.wasmBinary = t2[1]);
      let o = _.toString(), s = ["/* draco decoder */", r2, "", "/* worker */", o.substring(o.indexOf("{") + 1, o.lastIndexOf("}"))].join("\n");
      this.workerSourceURL = URL.createObjectURL(new Blob([s]));
    }), this.decoderPending;
  }
  _getWorker(e, t) {
    return this._initDecoder().then(() => {
      if (this.workerPool.length < this.workerLimit) {
        let e2 = new Worker(this.workerSourceURL);
        e2._callbacks = {}, e2._taskCosts = {}, e2._taskLoad = 0, e2.postMessage({ type: "init", decoderConfig: this.decoderConfig }), e2.onmessage = function(t2) {
          let r2 = t2.data;
          switch (r2.type) {
            case "decode":
              e2._callbacks[r2.id].resolve(r2);
              break;
            case "error":
              e2._callbacks[r2.id].reject(r2);
              break;
            default:
              console.error('THREE.DRACOLoader: Unexpected message, "' + r2.type + '"');
          }
        }, this.workerPool.push(e2);
      } else this.workerPool.sort(function(e2, t2) {
        return e2._taskLoad > t2._taskLoad ? -1 : 1;
      });
      let r = this.workerPool[this.workerPool.length - 1];
      return r._taskCosts[e] = t, r._taskLoad += t, r;
    });
  }
  _releaseTask(e, t) {
    e._taskLoad -= e._taskCosts[t], delete e._callbacks[t], delete e._taskCosts[t];
  }
  debug() {
    console.log("Task load: ", this.workerPool.map((e) => e._taskLoad));
  }
  dispose() {
    for (let e = 0; e < this.workerPool.length; ++e) this.workerPool[e].terminate();
    return this.workerPool.length = 0, "" !== this.workerSourceURL && URL.revokeObjectURL(this.workerSourceURL), this;
  }
};
function _() {
  let e, t;
  function r(e2, t2, r2, o, s, a) {
    let i = r2.num_points(), n = a.num_components(), d = (function(e3, t3) {
      switch (t3) {
        case Float32Array:
          return e3.DT_FLOAT32;
        case Int8Array:
          return e3.DT_INT8;
        case Int16Array:
          return e3.DT_INT16;
        case Int32Array:
          return e3.DT_INT32;
        case Uint8Array:
          return e3.DT_UINT8;
        case Uint16Array:
          return e3.DT_UINT16;
        case Uint32Array:
          return e3.DT_UINT32;
      }
    })(e2, s), l = n * s.BYTES_PER_ELEMENT, c = 4 * Math.ceil(l / 4), u2 = c / s.BYTES_PER_ELEMENT, h2 = i * l, f2 = i * c, w2 = e2._malloc(h2);
    t2.GetAttributeDataArrayForAllPoints(r2, a, d, h2, w2);
    let p2, m2 = new s(e2.HEAPF32.buffer, w2, h2 / s.BYTES_PER_ELEMENT);
    if (l === c) p2 = m2.slice();
    else {
      p2 = new s(f2 / s.BYTES_PER_ELEMENT);
      let e3 = 0;
      for (let t3 = 0, r3 = m2.length; t3 < r3; t3++) {
        for (let r4 = 0; r4 < n; r4++) p2[e3 + r4] = m2[t3 * n + r4];
        e3 += u2;
      }
    }
    return e2._free(w2), { name: o, count: i, itemSize: n, array: p2, stride: u2 };
  }
  onmessage = function(o) {
    let s = o.data;
    switch (s.type) {
      case "init":
        e = s.decoderConfig, t = new Promise(function(t2) {
          e.onModuleLoaded = function(e2) {
            t2({ draco: e2 });
          }, DracoDecoderModule(e);
        });
        break;
      case "decode":
        let o2 = s.buffer, a = s.taskConfig;
        t.then((e2) => {
          let t2 = e2.draco, i = new t2.Decoder();
          try {
            let e3 = (function(e4, t3, o3, s2) {
              let a2, i2, n2 = s2.attributeIDs, d = s2.attributeTypes, l = t3.GetEncodedGeometryType(o3);
              if (l === e4.TRIANGULAR_MESH) a2 = new e4.Mesh(), i2 = t3.DecodeArrayToMesh(o3, o3.byteLength, a2);
              else {
                if (l !== e4.POINT_CLOUD) throw new Error("THREE.DRACOLoader: Unexpected geometry type.");
                a2 = new e4.PointCloud(), i2 = t3.DecodeArrayToPointCloud(o3, o3.byteLength, a2);
              }
              if (!i2.ok() || 0 === a2.ptr) throw new Error("THREE.DRACOLoader: Decoding failed: " + i2.error_msg());
              let c = { index: null, attributes: [] };
              for (let o4 in n2) {
                let i3, l2, u2 = self[d[o4]];
                if (s2.useUniqueIDs) l2 = n2[o4], i3 = t3.GetAttributeByUniqueId(a2, l2);
                else {
                  if (l2 = t3.GetAttributeId(a2, e4[n2[o4]]), -1 === l2) continue;
                  i3 = t3.GetAttribute(a2, l2);
                }
                let h2 = r(e4, t3, a2, o4, u2, i3);
                "color" === o4 && (h2.vertexColorSpace = s2.vertexColorSpace), c.attributes.push(h2);
              }
              return l === e4.TRIANGULAR_MESH && (c.index = (function(e5, t4, r2) {
                let o4 = 3 * r2.num_faces(), s3 = 4 * o4, a3 = e5._malloc(s3);
                t4.GetTrianglesUInt32Array(r2, s3, a3);
                let i3 = new Uint32Array(e5.HEAPF32.buffer, a3, o4).slice();
                return e5._free(a3), { array: i3, itemSize: 1 };
              })(e4, t3, a2)), e4.destroy(a2), c;
            })(t2, i, new Int8Array(o2), a), n = e3.attributes.map((e4) => e4.array.buffer);
            e3.index && n.push(e3.index.array.buffer), self.postMessage({ type: "decode", id: s.id, geometry: e3 }, n);
          } catch (e3) {
            console.error(e3), self.postMessage({ type: "error", id: s.id, error: e3.message });
          } finally {
            t2.destroy(i);
          }
        });
    }
  };
}
export {
  m as DRACOLoader,
  p as DRACO_GLTF_CONFIG
};
//# sourceMappingURL=runtime-DRACOLoader-TQHJ6SJB-U4MSF57V.js.map
